# SSRF Policy

Remote fetch and callback operations use URL policy helpers that reject private ranges, localhost aliases, link-local addresses, and unsupported schemes unless explicitly configured for local development.
