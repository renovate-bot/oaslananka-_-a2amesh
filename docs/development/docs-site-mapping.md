# Docs Site Mapping

The VitePress site under `docs-site/` mirrors the canonical documentation topics from `docs/`. Package, CLI, protocol, security, and release pages must be kept in parity by `scripts/check-docs-package-parity.mjs` and `scripts/check-docs-commands.mjs`.
