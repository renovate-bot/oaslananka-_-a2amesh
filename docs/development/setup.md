# Development Setup

Linux/macOS:

```bash
corepack enable
pnpm install --frozen-lockfile
pnpm run verify
```

PowerShell:

```powershell
corepack enable
pnpm install --frozen-lockfile
pnpm run verify
```
