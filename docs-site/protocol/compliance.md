# Protocol Compatibility

The canonical compatibility matrix is maintained in `docs/protocol/compatibility.md` in the repository.

For executable compatibility profiles, see [Protocol Profiles](./profiles.md). The `official-a2a-v1.0` strict profile is emitted in conformance JSON reports together with row-level coverage metadata.
